# Base URL for the existing Catalyst Functions backend.
# All API requests in src/services/api.ts are built as `${VITE_API_BASE_URL}/<endpoint>/`.
VITE_API_BASE_URL=https://crimesightai-60075226836.development.catalystserverless.in/server

# Optional Leaflet tile configuration (only used if a Leaflet map is wired in later).
# VITE_MAP_TILE_URL=
# VITE_MAP_TILE_ATTRIBUTION=
# VITE_MAP_MAX_ZOOM=18
